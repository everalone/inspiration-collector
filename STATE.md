# 当前状态

项目 ID：P016
更新日期：2026-09-12

MVP 已完成并经真实数据验收，代码已开源：https://github.com/everalone/inspiration-collector （API key 走本地 `.env`，不入库）。项目目的见 `README.md`（含完整使用说明）。

## 2026-09-12 四次修订（分组 + 画廊 + 上传）

- **列表按文章分组**：父级 = 微信链接（标题 + 公众号 + AI 摘要，可折叠），子级 = 该文出的所有卡片，一眼看清每张卡出自哪篇。
- **详情页全文轮播画廊**：卡片定位裁剪图之下附文章全部轮播图，左右箭头/键盘切换 + 计数器 + 点击放大，截取不准时可自行翻看。
- **配置安全化**：API key 从 data/config.json 迁移到 `.env`（gitignore），仓库内只放 `.env.example` 模板。
- 梳理：移除 bootstrap tags 字段、allTags() 等死代码；.gitignore 修正（assets/、.zcode/）。

## 已完成（2026-09-12）

1. **抓取方案验证并落地**：微信文章两种页面格式均支持——传统图文（`#js_content` + `data-src` 懒加载）与新版"图片消息"轮播图（`picture_page_info_list` 逐 slide 提取最大尺寸图、meta description 取侧栏文字、`create_time` 取发布时间）。`/s/` 短链为永久 ID；数据中心 IP 会被风控，本机直连正常；`--offline` 另存 HTML 兜底。
2. **提取管线**：deepseek-flash 多模态（用户 key，本地 `data/config.json`），严格 JSON schema 把一篇文章拆成原子卡片；卡片类型 = payload 形态（palette/website/repo/prompt/note）+ 主题标签（AI 提议可编辑）。实测 7 篇真实文章产出 140 卡（54 组配色/22 个设计网站/19 条提示词/方法论笔记等）。
3. **校验闭环**：色卡 hex 与原图像素采样比对（超阈值标"待核对"）；网址探活（404/410/ENOTFOUND 判失效，反爬 403 不误判）；原图落盘供人工对照；卡片可编辑删除。
4. **卡片墙 UI（v2，按用户 Figma 稿重做）**：左列表 + 右详情 master-detail 布局；分类只有"类型"一层维度（全部/配色/网址/github/提示词/笔记/待核对），主题标签不再自动写入；列表卡片直接编辑/删除；详情面板贴来源原图（点击放大）对照；色卡支持原文描述字段（palette.desc）。全部交互实测通过。
5. **Electron 壳**：窗口 + 托盘常驻 + 剪贴板监听自动收集（去重提示）+ 完成通知 + 开机自启开关；核心服务以 `npx tsx cli.ts serve` 子进程方式拉起。静态资源 no-cache，改动即时生效。

## 2026-09-12 二次修订（用户反馈）

- 展示稿网址问题：设计风格参考类文章图中的"装饰性地址栏网址"会被忠实抄录但并非真实推荐链接。已在提取提示词中加规则（此类网址改填文章链接，github 项目地址除外），存量 20 张卡已批量修正为文章链接。
- 提示词展开/收起失效的根因是静态文件缓存了旧 JS——已给服务端加 no-cache，且新布局中详情页直接显示全文，该交互不复存在。

## 2026-09-12 三次修订（图片对应 + 栏宽）

- **bbox 定位**：提取时视觉模型为每张卡片输出在来源图中的矩形区域（0~1000 归一化，存 items.bbox）；服务端新增 `/api/crop` 按区域切图（immutable 缓存）；详情页只显示该卡片自己的那块图，点开看完整原图。deepseek-flash 的定位实测很准（配色网格逐格命中、设计风格逐行命中）。
- 7 篇文章已全部重跑补齐 bbox（156/156 覆盖），笔记卡不再错误对应第一张图。
- 左右栏宽可拖拽调整（分隔条，localStorage 记忆）。

## 下一步（候选，未排期）

1. electron-builder 打包安装包（当前为开发态运行）。
2. 手机端自动化入口：公众号转发接收（需订阅号+服务器）或局域网 HTTP 快捷指令。
3. 提取模型可切换/升级后 `node cli.ts reprocess <id>` 重跑。
4. 导出 Markdown/Notion；跨设备同步。

## 已知风险（更新）

- ~~微信抓取限制~~ 已验证可解，保留 `--offline` 兜底。
- 视觉提取对"设计稿假地址栏"会忠实抄录概念域名，探活会标记，需人工判断留存或删除。
- API key 在本地 `data/config.json`，已 gitignore，注意不要提交或分享。
